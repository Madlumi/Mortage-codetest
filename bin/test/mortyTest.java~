import org.junit.Test;

import static org.junit.Assert.*;
public class mortyTest{
	@Test
	public void tOne(){
		morty.main(new String[]{});
	}
	
	
	
	
	// double monthlyMortagePayment(double loanAmmount, double monthlyInterest, int payments)
	/*formula:
	 * E = U[b(1 + b)^p]/[(1 + b)^p - 1]
	 * E=montly payement
	 * b=monthly interest
	 * U=total loan
	 * p=number of payments
	 */
	 
	@Test
	public void testPowInts(){
		assertEquals(0,morty.monthlyMortagePayment(1000,.1,12),.0000001);
		
	}
}
